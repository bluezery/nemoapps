SET(BASIC "basic")
SET(LISTENER "listener")
SET(LAYOUT "layout")
SET(LAYOUT_DONUTBAR "layout_donutbar")
SET(LAYOUT_HISTOGRAM "layout_histogram")
SET(LAYOUT_DONUT "layout_donut")
SET(LAYOUT_AREA "layout_area")
SET(LAYOUT_DONUTARRAY "layout_donutarray")

ADD_EXECUTABLE(${BASIC}
	test_basic.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${BASIC}
	${NEMOAPPS_COMMON_LIBRARIES}
)

ADD_EXECUTABLE(${LISTENER}
	test_listener.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${LISTENER}
	${NEMOAPPS_COMMON_LIBRARIES}
)

ADD_EXECUTABLE(${LAYOUT}
	test_layout.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${LAYOUT}
	${NEMOAPPS_COMMON_LIBRARIES}
)

ADD_EXECUTABLE(${LAYOUT_DONUTBAR}
	test_layout_donutbar.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${LAYOUT_DONUTBAR}
	${NEMOAPPS_COMMON_LIBRARIES}
)

ADD_EXECUTABLE(${LAYOUT_HISTOGRAM}
	test_layout_histogram.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${LAYOUT_HISTOGRAM}
	${NEMOAPPS_COMMON_LIBRARIES}
)

ADD_EXECUTABLE(${LAYOUT_DONUT}
	test_layout_donut.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${LAYOUT_DONUT}
	${NEMOAPPS_COMMON_LIBRARIES}
)

ADD_EXECUTABLE(${LAYOUT_AREA}
	test_layout_area.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${LAYOUT_AREA}
	${NEMOAPPS_COMMON_LIBRARIES}
)

ADD_EXECUTABLE(${LAYOUT_DONUTARRAY}
	test_layout_donutarray.c
 	$<TARGET_OBJECTS:nemodavi>
)

TARGET_LINK_LIBRARIES(${LAYOUT_DONUTARRAY}
	${NEMOAPPS_COMMON_LIBRARIES}
)
