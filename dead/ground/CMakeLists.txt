SET(CONFXML "ground.conf")
SET(APPNAME "nemoground")
SET(GROUND_IMG_DIR "${IMG_DIR}/ground")
ADD_DEFINITIONS(-DAPPNAME="${APPNAME}")
ADD_DEFINITIONS(-DGROUND_IMG_DIR="${GROUND_IMG_DIR}")
ADD_DEFINITIONS(-DCONFXML="${CONFXML}")

ADD_EXECUTABLE(${APPNAME}
    ground.c
    )
ADD_DEPENDENCIES(${APPNAME} xemoutil nemowidget nemoui)

TARGET_LINK_LIBRARIES(${APPNAME}
    xemoutil
    nemowidget
    nemoui
    ${NEMOAPPS_COMMON_LIBRARIES}
	)

CONFIGURE_FILE(${CONFXML}.in ${CONFXML} @ONLY)
INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${CONFXML}
    DESTINATION ${ETC_DIR}
    )
INSTALL(TARGETS ${APPNAME}
    DESTINATION bin
    )
INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/images/
    DESTINATION  ${GROUND_IMG_DIR}
    )
