SET(SUBPROJECT "olympus")
SET(CONFXML0 "card.conf")
SET(CONFXML1 "background.conf")
SET(OLYMPUS_LOG_DIR  "${LOG_DIR}/${SUBPROJECT}")
SET(OLYMPUS_CARD_DIR "${DATA_DIR}/${SUBPROJECT}/card")
SET(OLYMPUS_BACK_DIR "${DATA_DIR}/${SUBPROJECT}/back")
SET(OLYMPUS_LINK_DIR "${DATA_DIR}/${SUBPROJECT}/link")

CONFIGURE_FILE(${CONFXML0}.in ${CONFXML0} @ONLY)
INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${CONFXML0}
    DESTINATION ${ETC_DIR}/${SUBPROJECT}/
    )
CONFIGURE_FILE(${CONFXML1}.in ${CONFXML1} @ONLY)
INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${CONFXML1}
    DESTINATION ${ETC_DIR}/${SUBPROJECT}/
    )

INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/card/
    DESTINATION  ${OLYMPUS_CARD_DIR}
    )
INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/back/
    DESTINATION  ${OLYMPUS_BACK_DIR}
    )
#INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/links/
#    DESTINATION  ${OLYMPUS_LINK_DIR}
#    )
