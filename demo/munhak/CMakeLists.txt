SET(SUBPROJECT "munhak")
SET(CONFXML0 "background.conf")
SET(CONFXML1 "overlay.conf")
SET(MUNHAK_OVERLAY_DIR "${DATA_DIR}/${SUBPROJECT}/overlay")
SET(MUNHAK_BG_DIR "${DATA_DIR}/${SUBPROJECT}/background")
SET(MUNHAK_ICON_DIR "${DATA_DIR}/${SUBPROJECT}/icon")

CONFIGURE_FILE(${CONFXML0}.in ${CONFXML0} @ONLY)
INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${CONFXML0}
    DESTINATION ${ETC_DIR}/${SUBPROJECT}/
    )
CONFIGURE_FILE(${CONFXML1}.in ${CONFXML1} @ONLY)
INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${CONFXML1}
    DESTINATION ${ETC_DIR}/${SUBPROJECT}/
    )

INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/overlay/
    DESTINATION  ${MUNHAK_OVERLAY_DIR}
    )
INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/background/
    DESTINATION  ${MUNHAK_BG_DIR}
    )
INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/icon/
    DESTINATION  ${MUNHAK_ICON_DIR}
    )
#INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/images/
#    DESTINATION  ${MUNHAK_IMG_DIR}
#    )
