SET(SUBPROJECT "munhak")
SET(CONFXML "background.conf")
SET(MUNHAK_ICON_DIR "${ICON_DIR}/${SUBPROJECT}")
SET(MUNHAK_IMG_DIR "${IMG_DIR}/${SUBPROJECT}")
SET(MUNHAK_LINK_DIR "${DATA_DIR}/${SUBPROJECT}")

CONFIGURE_FILE(${CONFXML}.in ${CONFXML} @ONLY)
INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${CONFXML}
    DESTINATION ${ETC_DIR}/${SUBPROJECT}/
    )

INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/icons/
    DESTINATION  ${MUNHAK_ICON_DIR}
    )
#INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/images/
#    DESTINATION  ${MUNHAK_IMG_DIR}
#    )
#INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/links/
#    DESTINATION  ${MUNHAK_LINK_DIR}
#    )
